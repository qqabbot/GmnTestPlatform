# Docker 部署时使用，与 application.yml 保持一致的数据源配置
# 激活方式: SPRING_PROFILES_ACTIVE=docker

server:
  port: 4000

spring:
  datasource:
    url: jdbc:mysql://10.48.0.13:3306/TestPlatform?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: insp_dev
    password: insp_dev@123
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    root: INFO
    com.testing.automation: INFO
    com.zaxxer.hikari: WARN
    com.mysql: WARN
    org.springframework.jdbc: WARN
