<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.testing.automation.Mapper.TestScenarioMapper">

    <resultMap id="BaseResultMap" type="com.testing.automation.model.TestScenario">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="project_id" property="projectId"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <!-- Association to Project if needed, typically lazy loaded or separate query -->
    </resultMap>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM test_scenarios ORDER BY id DESC
    </select>

    <select id="findByProjectId" resultMap="BaseResultMap">
        SELECT * FROM test_scenarios WHERE project_id = #{projectId} ORDER BY id DESC
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM test_scenarios WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO test_scenarios (name, description, project_id, created_at, updated_at)
        VALUES (#{name}, #{description}, #{projectId}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update">
        UPDATE test_scenarios
        SET name = #{name},
            description = #{description},
            project_id = #{projectId},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM test_scenarios WHERE id = #{id}
    </delete>

</mapper>
